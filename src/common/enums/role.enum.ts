export enum Role {
    // Administrador
    ADMIN = 'admin',
    // Usuario (Administrativo, Docente o Estudiante)
    USER = 'user',
    // Invitado (Postulantes a preuniversitario)
    GUEST = 'guest', // Rol por defecto
}